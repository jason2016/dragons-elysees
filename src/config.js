export const FEATURES = {
  delivery: false, // set to true to enable delivery mode
  intro: false,    // set to true to re-enable the koi opening animation (IntroOverlay). Kept in repo (IntroOverlay.jsx + public/intro.html); this slot is reserved for a future CultureIntro.
}
